<?xml version="1.0" encoding="UTF-8"?>
<project name="rules" default="run" basedir=".">

  <import file="analyzerCommon.xml" />

  <target name="run">
    <run.static.analysis
      language="${language}"
      ruleset="qaking_${language}.xml"
      kind="rules"
      reportFormat="xml"
    />
  </target>

</project>
